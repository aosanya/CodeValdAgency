# MVP Done — Completed Tasks

Completed tasks are removed from `mvp.md` and recorded here with their completion date.

| Task ID | Title | Completion Date | Branch | Notes |
|---------|-------|-----------------|--------|-------|
| MVP-AGENCY-001 | Library Scaffolding & Agency Model | 2026-03-18 | feature/AGENCY-001_library_scaffolding | Root package, AgencyManager interface, Agency model, lifecycle enforcement |
| MVP-AGENCY-002 | ArangoDB Backend | 2026-03-18 | feature/AGENCY-002_arangodb_backend | storage/arangodb: agency_details, agency_snapshots collections |
| MVP-AGENCY-003 | gRPC Service (AgencyService) | 2026-03-18 | feature/AGENCY-003_grpc_service | internal/server: all AgencyService RPCs wired to AgencyManager |
| MVP-AGENCY-004 | CodeValdCross Registration | 2026-03-18 | feature/AGENCY-004_cross_registration | internal/registrar: SharedLib heartbeat registrar; agency.created publisher |
| MVP-AGENCY-005 | Unit & Integration Tests | 2026-03-18 | feature/AGENCY-005_integration_tests | agency_test.go, storage/arangodb/storage_test.go, internal/server/integration_test.go |
| MVP-AGENCY-006 | Service-Driven Route Registration | 2026-03-18 | feature/AGENCY-007_agency_publishing | Routes declared in registrar.agencyRoutes(); registered with Cross on every heartbeat |
| MVP-AGENCY-008-A | Models refactor — standalone entity types | 2026-03-18 | main (direct commits) | Goal, Workflow, WorkItem, ConfiguredRole, AgencySnapshot, AgencyPublication as standalone entity types in models.go |
| MVP-AGENCY-008-B | Pre-delivered schema (`schema.go`) | 2026-03-19 | feature/AGENCY-008-B_pre-delivered-schema | `DefaultAgencySchema()` returning `types.Schema` with 7 TypeDefinitions; AgencySnapshot + AgencyPublication marked Immutable with dedicated StorageCollection |
| MVP-AGENCY-008-D | ArangoDB storage split (`entities.go`, `relationships.go`, `schemaops.go`) | 2026-03-19 | feature/AGENCY-008-D_arangodb-storage-split | entities.go: CreateEntity/GetEntity/UpdateEntity(immutable guard)/DeleteEntity(soft-delete)/ListEntities; relationships.go: CreateRelationship/GetRelationship/DeleteRelationship/ListRelationships/TraverseGraph (AQL graph); schemaops.go: SetSchema(auto-increment)/GetSchema/ListSchemaVersions; storage_test.go: 11 integration tests |
| MVP-AGENCY-008-E | Proto + gRPC handlers (`GetGoals`, `GetWorkflows`, `GetConfiguredRoles`) | 2026-03-19 | feature/AGENCY-008-E_grpc_goals_workflows_roles | Added GetGoals/GetWorkflows/GetConfiguredRoles RPCs to agency.proto; regenerated stubs; added server handlers and 9 new unit tests |
| MVP-AGENCY-008-F | `cmd/main.go` wiring — inject DataManager, seed schema on startup | 2026-03-19 | feature/AGENCY-008-F_cmd_wiring_schema_seed | `arangodb.NewBackend` replaces nil placeholders; `seedSchemaIfNeeded` idempotently seeds `DefaultAgencySchema()` on startup |
| MVP-AGENCY-008-C | `AgencyManager` refactor — entitygraph wrapper | 2026-03-19 | feature/AGENCY-008-C_agencymanager_entitygraph_wrapper | `agencyManager` now wraps `entitygraph.DataManager` instead of a bespoke `Backend`; convenience accessors delegate to `dm.ListEntities` |
| MVP-AGENCY-008-G | Test rewrite — `fakeDataManager` replacing `fakeBackend` | 2026-03-19 | feature/AGENCY-008-G_test_rewrite | `agency_test.go` rewritten around in-memory `fakeDataManager` implementing `entitygraph.DataManager`; old `fakeBackend` deleted |
| MVP-AGENCY-008-H | Type-specific URL params + `RoutesFromSchema` adoption | 2026-03-19 | feature/AGENCY-008-H_type_specific_url_params | `EntityIDParam` set on every schema TypeDefinition (`goalId`, `workflowId`, `draftId`, …); registrar calls `schemaroutes.RoutesFromSchema` for dynamic CRUD route generation |
| MVP-AGENCY-009-A | Models & errors update — drafts | 2026-03-19 | main (direct commits) | `AgencyDraft`, `AgencyDraftStatus`, `ErrAgencyNotPublished`, `ErrAgencyReadOnly`, `ErrDraftNotFound`, `ErrDraftNotOpen` shipped; `AgencyLifecycle` and lifecycle constants deleted; `Agency.Enabled` replaces `Status` |
| MVP-AGENCY-009-B | Schema update — drafts | 2026-03-19 | main (direct commits) | `AgencyDraft` TypeDefinition with `has_goal`/`has_workflow`/`has_configured_role` relationships and `belongs_to_agency` inverse; `Agency` gains `has_draft`; `Goal`/`Workflow`/`ConfiguredRole` gain optional `belongs_to_draft` |
| MVP-AGENCY-009-C | AgencyManager draft methods | 2026-03-19 | main (direct commits) | `CreateDraft`, `GetDraft`, `ListDrafts`, `UpdateDraftDescription`, `PromoteDraft`, `ArchiveDraft` shipped in `drafts.go`; `SetAgencyDetails` enforces `ErrAgencyReadOnly` once `Enabled == true` |
| MVP-AGENCY-009-D | ArangoDB storage — drafts | 2026-03-19 | main (direct commits) | `AgencyDraft` root entity routed to the `agency_drafts` collection via `TypeDefinition.StorageCollection`; draft sub-entities are dedicated types (`DraftGoal`, `DraftWorkflow`, `DraftWorkItem`, `DraftConfiguredRole`, `DraftInstruction`, `DraftDeliverable`, `DraftDeliverableResult`) routed to `agency_draft_entities`; `PromoteDraft` deep-copies sub-graph through entitygraph CRUD |
| MVP-AGENCY-009-E | gRPC handlers — drafts | 2026-03-19 | main (direct commits) | `CreateDraft`/`GetDraft`/`ListDrafts`/`UpdateDraftDescription`/`PromoteDraft`/`ArchiveDraft` RPCs added to `agency.proto`; handlers in `internal/server/server.go` |
| MVP-AGENCY-009-F | Tests — drafts | 2026-03-19 | main (direct commits) | 19 draft acceptance tests in `agency_test.go` covering fork-from-live, fork-from-draft, promote-replaces-live, archive-terminates, `ErrDraftNotOpen`, `ErrAgencyReadOnly`, `ErrAgencyNotPublished` paths |
| MVP-AGENCY-010 | RACI Schema: Role + ContextSource entity types | 2026-05-05 | feature/AGENCY-010_raci_schema | `Role`, `GitContextSource`, `CommContextSource`, `WorkContextSource` TypeDefinitions added to `DefaultAgencySchema()`; `has_role` edge on Agency; `has_context_source` edge on Role; model types and `ErrRoleNotFound`/`ErrContextSourceNotFound`/`ErrInvalidRegex` sentinels |
| MVP-AGENCY-011 | RACI AgencyManager: CRUD + MatchRoles query | 2026-05-05 | feature/AGENCY-011_raci_manager | `CreateRole`/`GetRole`/`ListRoles`/`UpdateRole`/`DeleteRole`, `AddContextSource`/`ListContextSources`/`RemoveContextSource`, `MatchRoles` in `raci.go`; regex validation via `ErrInvalidRegex`; `MatchRoles` returns `[]RoleMatch` ordered by ordinality |
| MVP-AGENCY-012 | RACI gRPC RPCs | 2026-05-05 | feature/AGENCY-012_raci_grpc | `Role`, `ContextSource`, `RoleMatch` messages + 9 RPCs added to `agency.proto`; stubs regenerated; all 9 handlers in `server.go`; `ErrRoleNotFound`→`NOT_FOUND`, `ErrContextSourceNotFound`→`NOT_FOUND`, `ErrInvalidRegex`→`INVALID_ARGUMENT` in `errors.go` |
| MVP-AGENCY-013 | RACI Cross Registration & Route Declarations | 2026-05-05 | feature/AGENCY-013_raci_cross_registration | 8 static `RouteInfo` entries added to `agencyRoutes()` in `registrar.go`: CreateRole, ListRoles, GetRole, UpdateRole, DeleteRole, AddContextSource, ListContextSources, RemoveContextSource with `PathBinding`s for `roleId`/`sourceId` |
| MVP-AGENCY-014 | RACI Unit & Integration Tests | 2026-05-05 | feature/AGENCY-014_raci_tests | 26 manager-level tests in `raci_test.go` (CreateRole, GetRole, ListRoles, UpdateRole, DeleteRole, AddContextSource, ListContextSources, RemoveContextSource, MatchRoles — all paths including regex validation, ordinality ordering, disabled-role exclusion, typed context source round-trips); 4 server-level gRPC tests in `internal/server/raci_server_test.go`; fixed `fakeDataManager.GetEntity` to respect soft-delete flag |
| FEAT-20260602-006 | Watchdog schema fields on Agency + WorkPlan — `inactivity_timeout_seconds` on Agency, `step_timeout` on WorkPlan | 2026-06-03 | feature/Dev-AGENCY-FEAT20260602006_workflow-run-watchdog | `schema.go`: `inactivity_timeout_seconds` (PropertyTypeInteger) on Agency TypeDefinition; `step_timeout` (PropertyTypeInteger) on WorkPlan TypeDefinition. `models.go`: `Agency.InactivityTimeoutSeconds int`, `WorkPlan.StepTimeout int`, `CreateWorkPlanRequest.StepTimeout`, `UpdateWorkPlanRequest.StepTimeout`. `raci.go`: `entityToWorkPlan` + `CreateWorkPlan` + `UpdateWorkPlan` round-trip. `agency.go`: `entityToAgency` + `SetAgencyDetails` round-trip. `internal/server/import_server.go`: YAML spec + `importSetDetails` carry the new Agency field. `go build` ✅ `go vet` ✅ `go test -race ./...` ✅ |
| FEAT-20260602-005 + 007 | Failure-pipeline schema fields on WorkPlan + Agency budget property | 2026-06-02 | feature/Dev-AGENCY-FEAT20260602-005_failure-pipeline-schema | `WorkPlan` gains `success_event`, `failure_event`, `on_failure_pipeline` PropertyDefinitions in `schema.go`; `Agency` gains `default_failure_pipeline_budget`. Threaded through `models.go` (`WorkPlan`/`Agency` structs), `raci.go` (`CreateWorkPlan`/`UpdateWorkPlan`/`entityToWorkPlan`), `agency.go` (`SetAgencyDetails` JSON contract + `entityToAgency`), and `internal/server/import_server.go` (YAML spec + `importSetDetails`). 3 new unit tests cover round-trip on Create/Update/SetAgencyDetails. |
| FEAT-20260608-001 | `EventFlowBranch` model — branches[] on EventFlow; schema.go comment; agency.json utility-app-builder branches updated | 2026-06-08 | feature/Dev-AGENCY-FEAT20260608002_flowchart-branch-edges | `schema.go` comment on `event_flows` property extended to document `EventFlowBranch` sub-object (`condition`, `emits_topic`, `emits_publisher`, `description`, `handler`) and branching rule (emits_topic must be "" when branches is non-empty). `agency.json` `functions.job.completed` flow gains three branches (compile-ok→git.branch.merged, issues+budget→work.todo.dispatched, budget-exhausted→work.task.failed). |
| FEAT-20260608-002 | `dev-agency-flowchart` skill — render branches[] as multiple Mermaid edges with branch description labels | 2026-06-08 | feature/Dev-AGENCY-FEAT20260608002_flowchart-branch-edges | `dev-agency-flowchart` skill already contained branching rendering logic; flowchart.md for utility-app-builder regenerated with correct full `description` labels for the three `functions.job.completed` branch edges. `schema.go` comment gains `trigger_publisher` and `emits_publisher` fields. `go build` ✅ `go vet` ✅ `go test -race ./...` ✅ |
| FEAT-20260605-002 | WorkPlan review step type — `review_step_type`, `review_trigger_topic`, `review_success_topic`, `review_failure_topic` fields on WorkPlan | 2026-06-08 | feature/Dev-AGENCY-FEAT20260605002_workplan-review-step | `schema.go`: 4 new PropertyDefinitions on WorkPlan TypeDefinition. `models.go`: 4 fields on `WorkPlan`, `CreateWorkPlanRequest`, `UpdateWorkPlanRequest`. `raci.go`: `CreateWorkPlan`/`UpdateWorkPlan`/`entityToWorkPlan` round-trip. `proto/agency.proto`: fields 19-22 on WorkPlan, Create/UpdateWorkPlanRequest. `gen/go/` regenerated. `server.go`: handler + `workPlanToProto` updated. `import_server.go`: YAML spec + upsert props updated. 2 new server-layer tests. `go test -race ./...` ✅ |
| FEAT-20260609-002 | Per-Workflow `event_flows` field + multi-file flow import (`flows_<workflow.code>.json`) | 2026-06-09 | feature/Dev-AGENCY-FEAT20260609002_per-workflow-event-flows | `schema.go`: `event_flows` PropertyDefinition on Workflow + DraftWorkflow. `models.go`: `Workflow.EventFlows`. `proto/agency.proto`: `Workflow.event_flows = 4`; `gen/go/` regen. `internal/server/import_server.go`: `importWorkflowSpec.EventFlows` decoded from inline JSON/YAML object, marshalled to JSON string on DraftWorkflow upsert; deprecation warning when only legacy top-level form is supplied. `agency.go`/`internal/server/server.go`: `entityToWorkflow` + `workflowsToProto` surface the new field. `PromoteDraft` already carries DraftWorkflow props through via `copyPropsExcluding(draft_ref_code)`. New `internal/server/import_server_test.go` covers stored-path JSON round-trip + omitted-path no-prop. `go build` ✅ `go vet` ✅ `go test -race ./...` ✅ |
| FEAT-20260609-001 | Add `task-split-handler` work plan to utility-app-builder agency.json (consumes `task.request-split`, emits `task.subtask-created`) | 2026-06-09 | feature/Dev-AGENCY-FEAT20260609001_task-split-handler-workplan | `models.go`/`schema.go`/`proto/agency.proto`/`raci.go`: new `agent_code` property on WorkPlan (symbolic AIAgent code resolved by CodeValdAI bootstrap at startup) + new `reviewer_role_code` on DraftDeliverable. `internal/server/import_server.go`: accepts `assigned_roles` (plural), `function_params` as object (JSON-serialised before storage), and `reviewer_role` on deliverables. `gen/go/` regenerated. `CodeValdImplementations/Agencies/utility-app-builder/agency.json`: declares `task-split-handler` work plan (trigger `task.request-split`, handler `codevaldai`, agent `deepseek-v4-task-splitter`, emits `task.subtask-created`) + `deepseek-v4-task-splitter` AIAgent + RACI entry. `go build` ✅ `go vet` ✅ `go test -race ./...` ✅ |
